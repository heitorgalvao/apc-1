#include <stdio.h>
int main() {
    int dia = 0;
    printf("digite o dia da semana(numericamente):");
    scanf("%i",&dia);
    if (dia = 1 );
    printf("Hoje e Domingo");
        if (dia = 2 );
        printf("Hoje e Segunda")
    return 0;
}